import 'package:flutter/material.dart';

class LadingPage extends StatelessWidget {
  const LadingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Lading Page'),
      ),
    );
  }
}
